# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'

db:
  adapter: sqlite3
  database: stocks/2015.sqlite3
  pool: 5
  timeout: 5000
